<h1>Users you have blocked:</h1>
<br>
<% @blocks.each do |block| %>
	<div class='row'>
		<div class='col-sm-2 text-center'>
			<% size = '100x100' %>
			<% if block.blocked.image_url %>
				<%= image_tag(block.blocked.image_url, size: size, :alt => "profile image") %><br>
			<% else %>
				<%= image_tag("/images/blank_profile.png", size: size, :alt => "login with Facebook to get an image") %><br>
			<% end %>
		</div>
		<div class='col-sm-3'>
			<strong>
			<%= block.blocked.first_name %>
			<%= block.blocked.last_name %>
			<br>
			<%= block.blocked.email %>
			</strong>
			<br><br>
			<%= button_to "Unblock this user", user_blockeduser_path(block.blocker_id,block.id), method: :delete , :class => 'btn btn-danger'%></p>
		</div>
	</div>
<hr/>
<% end %>
<%= link_to 'Back', root_path %>



